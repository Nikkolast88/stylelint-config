{
  "name": "@uni-daily/stylelint-config-monorepo",
  "version": "0.0.4",
  "private": "true",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "release": "changelogen && bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "bumpp": "^8.2.1",
    "changelogen": "^0.4.0",
    "eslint": "^8.29.0",
    "prettier": "^2.8.1"
  }
}
